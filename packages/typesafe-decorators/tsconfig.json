{
  "extends": "../../shared/tsconfigs/base.tsconfig.json",
  "compilerOptions": {
    "outDir": "./lib",
    "removeComments": false // TODO: it seems rollup-plugin-tsc still removes comments, need to add them back manually
  }
}
